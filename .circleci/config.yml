version: 2
jobs:
  build:
    working_directory: ~/app
    docker:
      - image: circleci/openjdk:8-jdk-browsers
        environment:
          LANG: C.UTF-8

    steps:
      - checkout
      - run:
          name: Log stuff
          command: echo "Hello world!"
